name: "Terraform CI/CD"

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:
    inputs:
      action:
        description: 'Accion a ejecutar en Terraform'
        required: true
        default: 'apply'
        type: choice
        options:
          - apply
          - destroy

jobs:
  terraform:
    name: "Terraform Execution"
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.8.0

      - name: Check code formatting
        run: terraform fmt -check -recursive

      - name: Initialize Terraform
        run: terraform init

      - name: Validate syntax
        run: terraform validate

      # Escaneo de seguridad estático de Infraestructura como Código (IaC)
      - name: Run Trivy IaC Security Scanner
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          scan-type: 'fs'
          scan-ref: '.'
          format: 'table'
          severity: 'CRITICAL,HIGH'
          exit-code: '0' # Con 0 muestra el reporte sin bloquear el pipeline mientras aprendemos

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: eu-west-1

      - name: Run Terraform Plan
        if: "${{ github.event.inputs.action != 'destroy' }}"
        run: terraform plan -input=false

      - name: Run Terraform Apply
        if: "${{ github.event_name == 'push' || github.event.inputs.action == 'apply' }}"
        run: terraform apply -auto-approve -input=false

      - name: Run Terraform Destroy
        if: "${{ github.event.inputs.action == 'destroy' }}"
        run: terraform destroy -auto-approve -input=false